# Obsidian per-machine state (don't sync workspace layout / caches across machines)
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache
.trash/

# Local-only archive snapshot — kept on the laptop, never synced to GitHub/lxplus
Archive/

# Overleaf analysis-note clone — has its own Overleaf git remote, never push to this GitHub repo
Projects/HToWW/AnalysisNote/

# Private, machine-local notes (keyring/secrets/paths, laptop-side workflows) — never synced
References/Private/

# OS cruft
.DS_Store
.DS_Store?
Thumbs.db
ehthumbs.db
Desktop.ini
nul

# macOS AppleDouble / resource-fork sidecars (created over sshfs and by the Mac Finder)
._*
.AppleDouble
.LSOverride
.Spotlight-V100
.Trashes
.fseventsd
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Linux / editor cruft
*~
.directory
.Trash-*

# Marp slide previews (regenerable: npx @marp-team/marp-cli <deck>.md --images png)
Projects/HToWW/argmax-kinematics-2026-08-07/slide.*.png

# TMVA lepton-MVA weight XMLs -- large regenerable inputs, master copy on EOS
# at /eos/user/c/cgupta/HToWW/leptonmva/
Muon-mvaTTH.*.weights.xml
Electron-mvaTTH.*.weights*.xml

# Semantic search index (regenerable: scripts/rag/vault-search --build)
.obsidian-semantic-index.json

# Python bytecode
__pycache__/
*.pyc

# Multi-source RAG index + rebuild lock/log (regenerable: scripts/rag/build_index.py)
.vault-rag-index.json
.vault-rag-index.lock
.vault-rag-index.log

# Overleaf clone is its own git repo (pushes to git.overleaf.com)
6a5a2a3da030d5296033a920/
