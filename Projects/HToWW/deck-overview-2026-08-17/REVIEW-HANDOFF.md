---
tags: [reference]
status: active
date: 2026-08-19
source: laptop
---

# REVIEW REQUEST — H+c analysis status deck

**For a reviewing agent.** The deck is being actively edited by another agent in
parallel; review the committed PDF, do **not** edit the markdown.

## What to review

```
Projects/HToWW/deck-overview-2026-08-17/analysis-overview.pdf   (51 slides)
Projects/HToWW/deck-overview-2026-08-17/analysis-overview.md    (source — DO NOT EDIT)
```

Commit `9f618a4` on `master`.

## Audience and purpose

A **status update to physicists already familiar with this analysis** — supervisor
and analysis-group colleagues. They know what H+c is, what a datacard is, what
autoMCStats does. They do **not** need combine tutorials.

The deck must survive hostile questions from people who know the analysis better
than the presenter does.

## What I want from the review

Go **slide by slide**. For each, answer:

1. **Is the claim on this slide true and supported?** Flag anything that reads as
   asserted rather than measured.
2. **Would an expert audience find it necessary?** Flag slides that are filler,
   duplicated, or below the audience's level.
3. **Is anything missing that they would immediately ask about?**
4. **Does the plot actually show what the caption says?** One mislabelled plot has
   already been caught this way (`B3_shapes_SR_hplusc` is shape *variations*, not a
   template) — assume there are more.

Report as a numbered list keyed to slide number, most serious first. **Do not fix
anything** — the other agent is editing the same file.

## Known-weak areas — please attack these specifically

| # | area | concern |
|---|---|---|
| 1 | **the 1034 limit** | deliberately labelled "under investigation". Is that honest enough, or does it read as hiding something? |
| 2 | **stat/syst split** | 62% stat / 38% syst, from stat-only 641 vs full 1034. Is presenting a *ratio of limits* as a "statistical fraction" defensible? I am not certain it is. |
| 3 | **impacts plot** | `r̂ = 0 +500/−500` appears in the plot header; the underlying fit is `r̂ = 0.99996 −513/+483`. The header is a plotImpacts rounding artifact — but will the audience read it as a broken fit? |
| 4 | **AN comparison** | REMOVED from the deck after three failed attempts to reproduce AN-23-102 Table 17's metric (quadrature vs linear vs σ(r) vs limit-fraction). Should some comparison be restored, and on what definition? |
| 5 | **prefit vs postfit** | nearly identical, because it is an Asimov fit. Does showing it add anything, or does it invite "why is there no difference"? |
| 6 | **Bkg-Higgs** | 0.2% impact vs the AN's 7.6%. Explained: the per-event `higgs_plus_c` weight is implemented but not yet active, so a 1.066 placeholder lnN stands in. Is that explained clearly enough on the slides? |

## Context the reviewer needs

**Current status.** Expected UL **1034** on 2022postEE (26.7 fb⁻¹), stat-only **641**.
Down from 1371 through: negrw → sumw normalisation fix → LOWESS off → 2D c-tag SF →
W+jets jet-binned samples (−126).

**The W+jets change** is the most recent result: V+jets `n_eff` 280 → 1170 (×4.18),
relative statistical error 5.98% → 2.92%, while the **rate moved only +1.0%** — the
signature of a statistics gain rather than a normalisation bug.

**Known caveat on that number:** tt (+4.8%) and st (+3.8%) also moved between the two
cards because the newer one re-merged all 887 partitions. Checked per-process: the
`n_eff` gain is confined to V+jets (other processes 0.87–1.13×, moving in *both*
directions), so it is re-merge jitter, not a systematic shift. Both cards were
re-verified independently at 1160 and 1034.

**Three systematics are implemented but not yet active** (`higgs_plus_c`, `top_pt`,
trigger SFs) because the weight columns require reprocessing the affected samples.
The deck says "under investigation" rather than spelling this out — deliberate,
per the user's instruction.

**Deliberately excluded, with reasons on the slides:** muon reco SF (absent from both
HiggsDNA and hh2bbww), PU jet ID, UE/tune, `hdamp`, `mtop` — the last three need
dedicated samples and cannot be weights.

## Supporting material

| file | what |
|---|---|
| `decks-2026-07-31/negrw-deck.pdf` | 57 slides, negative-weight reweighting |
| `decks-2026-07-31/ctag-sf-deck.pdf` | 34 slides, 2D c-tag scale factors |
| `2026-08-14-wjets-postprocess-to-limit.md` | the 1034 result and its caveats |
| `2026-07-24-systematics-master-list.md` | full systematics inventory |
| `HToWW-systematics-inventory.csv` | 40 rows, machine-readable |
| `strategy-2026-08-10/2026-08-11-verified-against-HiggsDNA-and-hh2bbww.md` | what was fixed vs the reference frameworks |

## Still outstanding (not a review item)

Data/MC comparison plots are being generated by `run_postprocess.py --plot` and are
not yet in the deck.
